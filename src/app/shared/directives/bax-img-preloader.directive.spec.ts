import { BaxImgPreloaderDirective } from './bax-img-preloader.directive';

describe('BaxImgPreloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BaxImgPreloaderDirective();
    expect(directive).toBeTruthy();
  });
});
