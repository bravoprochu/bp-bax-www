import { FillChangeDirective } from './fill-change.directive';

describe('FillChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FillChangeDirective();
    expect(directive).toBeTruthy();
  });
});
