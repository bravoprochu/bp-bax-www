import { DetectIntersectionDirective } from './detect-intersection.directive';

describe('DetectIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
